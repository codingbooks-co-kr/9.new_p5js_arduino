// do-while문을 사용하여 다섯 개의 평행선 그리기

function setup() {
  createCanvas(300, 300);
}

function draw() {
  background(0);
  let y = 50;
  do {
    stroke(255);
    line(0, y, width, y);
    y += 50;
  } while (y < height);
}