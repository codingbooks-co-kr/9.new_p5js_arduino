// 배열의 선언,생성,초기화를 따로 처리

let x = [];

function setup() {
  createCanvas(300, 300);
  x[0] = -50;
  x[1] = 0;
  x[2] = 50;
}

function draw() {
  background(0);
  for (let i = 0; i < 3; i++) {
    ellipse(x[i], (80*i)+80, 50, 50);
    x[i]++;
  }
}