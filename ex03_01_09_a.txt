// ❶ 전역·지역 변수 사용

// 전역변수
let s = 0, r = 10;

function setup() {
  createCanvas(300, 300);
}

function draw() {
  background(0);
  // 지역변수
  let s = 150;
  rectMode(CENTER);
  rect(s, s, r, r);
  r++;
}