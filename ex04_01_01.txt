// 한쪽으로 반사하는 원

let x = 0, velocity = 1;

function setup() {
  createCanvas(300, 300);
}

function draw() {
  background(0);
  ellipse(x, height/2, 30, 30);
  x += velocity;
  if (x > width) {
    velocity *= -1;
  }
}