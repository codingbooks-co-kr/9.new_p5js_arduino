// 회전 운동

let theta = 0.0, r = 150.0;

function setup() {
  createCanvas(300, 300);
  background(0);
}

function draw() {
  let x = width/2 + r * cos(theta);
  let y = height/2 + r * sin(theta);
  ellipse(x, y, 30, 30);
  theta += 0.05;
}