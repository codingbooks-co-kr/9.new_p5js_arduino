// 배열의 선언,생성,초기화를 한 번에 처리

let x = [-50, 0, 50];

function setup() {
  createCanvas(300, 300);
}

function draw() {
  background(0);
  for (let i = 0; i < 3; i++) {
    ellipse(x[i], (80*i)+80, 50, 50);
    x[i]++;
  }
}