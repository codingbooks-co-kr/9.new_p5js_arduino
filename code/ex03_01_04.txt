// 문자 세 개 출력

let c1 = 'A', c2 = 'B', c3 = 'C';
let x = 70, y = 70;

function setup() {
  createCanvas(300, 300);
  background(200);

  textSize(50);
  text(c1, x, y);
  text(c2, x*2, y*2);
  text(c3, x*3, y*3);
}