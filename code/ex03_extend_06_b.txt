let c = '가';
let s = '안녕, 자바스크립트!';
let x = 50, y = 50;

function setup() {
  createCanvas(350, 200);
  textFont('굴림체', 30);
  background(200);

  fill(255, 0, 0);
  text(c, x, y);
  text(s, x, y * 2);
  text(s, x, y * 3);
}