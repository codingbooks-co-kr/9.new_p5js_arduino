// 가로선 그리기2
// 변수 사용

let x1;	   // 변수 선언
let x2;
let y;

x1 = 50;  // 변수 초기화
x2 = 250;
y = 60;

function setup() {
  createCanvas(300, 300);
  background(200);

  line(x1, y, x2, y);
  line(x1, y*2, x2, y*2);
  line(x1, y*3, x2, y*3);
  line(x1, y*4, x2, y*4);
}