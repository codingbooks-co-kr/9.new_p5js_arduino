// 작은 원들로 출력 화면을 가득 채우기

function setup() {
  createCanvas(300, 300);
}

function draw() {
  background(0);
  for (let y = 0; y <= height; y +=30) {
    for (let x = 0; x <= width; x += 30) {
      fill(255, 255, 0);
      ellipse(x, y, 30, 30);
    }
  }
}