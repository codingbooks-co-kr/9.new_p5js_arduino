// 원을 일정 속도로 이동1

let x = 200, y = 200;
let velocity_x = 1, velocity_y = 1;

function setup() {
  createCanvas(400, 400);
  background(0);
}

function draw() {
  ellipse(x, y, 30, 30);
  x += velocity_x;
  y += velocity_y;
  console.log(frameCount + ' : ' + frameRate());
}